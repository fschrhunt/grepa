[package]
name = "grepa"
version = "0.2.0"
edition = "2021"
description = "Small safe CLI for the grep.app MCP server"
license = "MIT"
repository = "https://github.com/fschrhunt/grepa"

[dependencies]
lexopt = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "3", features = ["json"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
